<div class="agencia-info">
  <div class="fila">
    <span class="titulo">Agencia Imprime:</span>
    <span class="codigo-agencia">{{ agencia.codigoAgenciaImprime }}</span>
    <span class="nombre-agencia">{{ agencia.nombreAgenciaImprime }}</span>
  </div>
  <div class="fila">
    <span class="titulo">Agencia Apertura:</span>
    <span class="codigo-agencia">{{ agencia.codigoAgenciaApertura }}</span>
    <span class="nombre-agencia">{{ agencia.nombreAgenciaApertura }}</span>
  </div>
</div>


.agencia-info .fila {
  display: flex; /* Alinea los elementos de la fila horizontalmente */
  align-items: center; /* Centra los elementos verticalmente en la fila */
  margin-bottom: 10px; /* Espacio entre filas para mejor legibilidad */
}

.agencia-info .titulo {
  font-weight: bold; /* Títulos en negrita para destacar */
  margin-right: 5px; /* Espacio después del título */
}

.agencia-info .codigo-agencia, .agencia-info .nombre-agencia {
  margin-right: 5px; /* Espacio entre el código y el nombre */
}

