<h1>Consulta de Tarjeta</h1>
<div class="card">
  <div class="card-body">
    <p>Agencia Apertura:</p>
    <p>{{ agenciaAperturaNombre }}</p>
    <p>{{ agenciaAperturaCodigo }}</p>
    <p>Agencia Imprime:</p>
    <p>{{ agenciaImprimeNombre }}</p>
    <p>{{ agenciaImprimeCodigo }}</p>
  </div>
</div>
<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Número</th>
        <th>Fecha Emisión</th>
        <th>Fecha Vencimiento</th>
        <th>Número Cuenta</th>
      </tr>
    </thead>
    <tbody>
      <tr *ngFor="let tarjeta of tarjetas" (click)="seleccionarFila(tarjeta)">
        <td>{{ tarjeta.nombre }}</td>
        <td>{{ tarjeta.numero }}</td>
        <td>{{ tarjeta.fechaEmision }}</td>
        <td>{{ tarjeta.fechaVencimiento }}</td>
        <td>{{ tarjeta.numeroCuenta }}</td>
      </tr>
    </tbody>
  </table>
</div>
<div class="modal" [ngClass]="{'modal-visible': modalVisible}">
  <div class="modal-content">
    <div class="modal-header">
      <span class="modal-title">Vista Previa Impresión</span>
      <button type="button" class="close" (click)="modalVisible = false">×</button>
    </div>
    <div class="modal-body">
      <div [innerHTML]="vistaPrevia"></div>
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-primary">Imprimir</button>
    </div>
  </div>
</div>
